 <a href="{{ path ('post_show', {slug: post.slug, id: post.id }) }}">
 <h2>{{ post.title}}</h2>
 </a>
    <p> Par: <em>{{post.author}} - </em>Le: <em><small>{{ post.getFormattedDate(local)}}</small></em></p>
 <p>{{ post.getContentPreview(120) }}</p>